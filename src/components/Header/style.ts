import styled from "styled-components";

export const StyledHeader = styled.header`
`
export const StyledH1 = styled.h1`
    text-align: center;
    margin-bottom: 1rem;
    color: ${(props)=> props.theme.color.sectionInput.title};
    font-size : 1.4rem;
    font-weight : 600
`